[project]
name = "homelab-dns"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pulumi>=3.247.0",
    "pulumi-cloudflare>=6.17.0",
    "orjson>=3.11.9",

    # Workspace
    "homelab-common",
    "homelab-context",
    "homelab-model",
]

[build-system]
requires = ["uv_build>=0.11.19,<0.12.0"]
build-backend = "uv_build"
